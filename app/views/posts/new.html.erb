<%= form_for :post, url: posts_path do |f| %>
   <% if @post.errors.any? %>
      <div >
        <h2>
          <%= pluralize(@post.errors.count, "error") %> prohibited
          wrong:
        </h2>
        <ul>
          <% @post.errors.full_messages.each do |msg| %>
            <li class="error_explanation">The <%= msg %>.</li>
          <% end %>
        </ul>
      </div>
    <% end %>
 
  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.file_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

